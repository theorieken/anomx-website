"use client";

import Link from "next/link";
import { useLanguage } from "@/components/language-switcher";
import { CopyCommand, Experience } from "@/components/experience";

export function DocumentationPage() {
    const de = useLanguage() === "de";
    const sections = [
        ["quickstart", de ? "Schnellstart" : "Quick start"], ["providers", de ? "Modelle & Anbieter" : "Models & providers"], ["modes", de ? "Ausführungsmodi" : "Execution modes"], ["commands", de ? "Befehle & Skills" : "Commands & skills"], ["platform", de ? "Plattform verbinden" : "Platform connection"], ["background", de ? "Hintergrundagent" : "Background agent"], ["storage", de ? "Sitzungen & Speicher" : "Sessions & storage"], ["package", "Python API"]
    ];
    const modes = de ? [
        ["Plan", "Nur Lesezugriffe. Keine Änderungen an Dateien, Prozessen oder Plattformzustand."], ["Standard", "Voreinstellung. Noch nicht als freigegeben gespeicherte Kommandos benötigen Zustimmung."], ["Automatic", "Lesezugriffe laufen automatisch. Der Risikoklassifikator gibt Kommandos mit niedrigem Risiko frei; mittleres und hohes Risiko benötigen Zustimmung."], ["Autonomous", "Umgeht die Kommandorichtlinie und Freigabedialoge, auch für Host-Steuerung und sudo. Wählen Sie diesen Modus nur bewusst in einer geeigneten Umgebung."]
    ] : [
        ["Plan", "Read operations only. No changes to files, processes, or platform state."], ["Standard", "Default mode. Commands not already remembered as approved require permission."], ["Automatic", "Read operations run automatically. The risk classifier approves low-risk commands; medium- and high-risk commands require permission."], ["Autonomous", "Bypasses the command policy and approval prompts, including host-control and sudo commands. Choose deliberately in an appropriate environment."]
    ];
    return <Experience className="docs-experience"><div className="doc-layout content-width"><aside className="doc-sidebar"><span>ANOMX DOCUMENTATION</span>{sections.map(([id, label]) => <a key={id} href={`#${id}`}>{label}</a>)}</aside><article className="doc-content"><p className="kicker">CLI AGENT · PYTHON · PLATFORM</p><h1>{de ? "Vom Prompt\nzum Verständnis." : "From first prompt\nto system insight."}</h1><p className="doc-intro">{de ? "Installation, Modelle und Arbeitsabläufe für den Anomx-Agenten. Die folgenden Beispiele beziehen sich auf die Paketversion 0.2.34. Die öffentliche API wird aktiv weiterentwickelt." : "Installation, models, and workflows for the Anomx agent. These examples cover package version 0.2.34. The public API is under active development."}</p>
        <section className="doc-section" id="quickstart"><h2>01. {de ? "Schnellstart" : "Quick start"}</h2><p>{de ? "Sie benötigen Python 3.11 oder neuer und ein interaktives Terminal. Installieren Sie das Paket in einer virtuellen Umgebung:" : "You need Python 3.11 or newer and an interactive terminal. Install the package in a virtual environment:"}</p><CopyCommand command={'python3 -m venv .venv\nsource .venv/bin/activate\npython -m pip install --upgrade anomx'} /><p>{de ? "Starten Sie den Agenten im gewünschten Arbeitsverzeichnis:" : "Start the agent in the directory you want to work in:"}</p><CopyCommand command="anomx" /><ol><li>{de ? "Verbinden Sie beim Start einen Modellanbieter. Sie können ihn später unter /config → Manage Backends ändern." : "Connect a model provider during onboarding. Change it later with /config → Manage Backends."}</li><li>{de ? "Wählen Sie mit /model ein verfügbares Modell. Beginnen Sie im Standard-Modus." : "Choose an available model with /model. Start in Standard mode."}</li><li>{de ? "Beschreiben Sie Ihre Frage, zum Beispiel: „Untersuche die CSV-Dateien in diesem Ordner. Welche Kanäle zeigen ungewöhnliches Verhalten?“" : "Describe your question, for example: “Inspect the CSV files in this directory. Which channels show unusual behavior?”"}</li></ol><p className="doc-note">{de ? "Für lokale Analyse ist keine Plattformverbindung nötig. Ein konfigurierter Modellanbieter wird benötigt. Cloud-Anbieter können eigene Zugangsbedingungen und Nutzungskosten haben." : "Local analysis does not require a platform connection. A configured model provider is required. Cloud providers may have their own access requirements and usage costs."}</p></section>
        <section className="doc-section" id="providers"><h2>02. {de ? "Modelle & Anbieter" : "Models & providers"}</h2><p>{de ? "Nutzen Sie /config → Manage Backends für den Anbieterzugang und /model für die Modellauswahl. /effort verändert den Reasoning-Aufwand, sofern der Anbieter dies unterstützt." : "Use /config → Manage Backends to configure provider access and /model to choose a model. /effort changes reasoning effort where supported by the provider."}</p><div className="doc-table-wrap"><table className="doc-table"><thead><tr><th>{de ? "Anbieter" : "Provider"}</th><th>CLI key</th></tr></thead><tbody>{[["OpenAI","openai"],["Anthropic","anthropic"],["Kimi","kimi"],["Ollama","ollama"],["DESY Assistant","desy"],["JSC Blablador","blablador"]].map(([name,key]) => <tr key={key}><td>{name}</td><td><code>{key}</code></td></tr>)}</tbody></table></div><CopyCommand command="anomx --provider openai" /><p>{de ? "Für Ollama muss der lokale Dienst laufen und das gewählte Modell installiert sein. Ersetzen Sie MODEL_ID durch ein Modell, das Ihr Anbieter tatsächlich bereitstellt:" : "For Ollama, the local service must be running and the chosen model installed. Replace MODEL_ID with a model actually served by your provider:"}</p><CopyCommand command="anomx --ollama --model MODEL_ID" /><p>{de ? "Startparameter: --provider, --model, --ollama, --home, --print-home, --no-color, --version. ANOMX_PROVIDER und ANOMX_MODEL setzen Startwerte; explizite Parameter haben Vorrang." : "Startup flags: --provider, --model, --ollama, --home, --print-home, --no-color, --version. ANOMX_PROVIDER and ANOMX_MODEL supply startup defaults; explicit flags take precedence."}</p></section>
        <section className="doc-section" id="modes"><h2>03. {de ? "Ausführungsmodi" : "Execution modes"}</h2><p>{de ? "Mit Shift+Tab wechseln Sie zwischen vier interaktiven Modi. Der aktive Modus wird in der Eingabezeile angezeigt." : "Press Shift+Tab to cycle through four interactive modes. The active mode appears in the prompt bar."}</p><div className="doc-table-wrap"><table className="doc-table"><thead><tr><th>{de ? "Modus" : "Mode"}</th><th>{de ? "Verhalten" : "Behavior"}</th></tr></thead><tbody>{modes.map(([name,body]) => <tr key={name}><td>{name}</td><td>{body}</td></tr>)}</tbody></table></div><p className="doc-note">{de ? "Background gehört nicht zum interaktiven Moduswechsel. Er wird durch geplante Plattformläufe aktiviert. Frühere Bezeichnungen wie Observer, Confirm und Recommend sind keine aktuellen interaktiven Modi." : "Background is not part of the interactive mode cycle. It is activated by scheduled platform runs. Earlier labels such as Observer, Confirm, and Recommend are not current interactive modes."}</p></section>
        <section className="doc-section" id="commands"><h2>04. {de ? "Befehle & Skills" : "Commands & skills"}</h2><div className="doc-table-wrap"><table className="doc-table"><thead><tr><th>{de ? "Befehl" : "Command"}</th><th>{de ? "Funktion" : "Purpose"}</th></tr></thead><tbody>{(de ? [["/new","Neue Sitzung starten"],["/rename","Aktuelle Sitzung umbenennen"],["/config","Backends, Plattform, Skills, Erinnerungen, Anweisungen, Sandbox und Einstellungen verwalten"],["/model","Modell wählen"],["/effort","Reasoning-Aufwand wählen"],["/feedback","Feedback an die verbundene Anomx-Plattform senden"],["/exit","Anomx beenden"]] : [["/new","Start a new session"],["/rename","Rename the current session"],["/config","Manage backends, platform, skills, memories, instructions, sandbox, and settings"],["/model","Choose a model"],["/effort","Choose reasoning effort"],["/feedback","Send feedback to the connected Anomx platform"],["/exit","Exit Anomx"]]).map(([name,body]) => <tr key={name}><td><code>{name}</code></td><td>{body}</td></tr>)}</tbody></table></div><h3>{de ? "Wiederkehrende Arbeit als Skill" : "Repeatable work as a skill"}</h3><p>{de ? "Verwalten Sie eigene Skills über /config → Manage Skills. Plattformbezogene Skills umfassen manage-data, retrieve-data, manage-jobs, manage-systems, manage-recommendations und use-anomx-api. Ihre Verfügbarkeit hängt von der Plattformverbindung ab." : "Manage your own skills with /config → Manage Skills. Platform skills include manage-data, retrieve-data, manage-jobs, manage-systems, manage-recommendations, and use-anomx-api. Their availability depends on the platform connection."}</p><h3>{de ? "Arbeit unterbrechen" : "Interrupt work"}</h3><p>{de ? "Während der Agent arbeitet, können Sie mit Ctrl+C oder Ctrl+X eine Unterbrechung anfordern. Bestätigen Sie sie, wenn die Oberfläche dazu auffordert. Esc führt zur vorherigen Ansicht zurück." : "While the agent is working, Ctrl+C or Ctrl+X requests an interruption. Confirm it when prompted by the interface. Esc returns to the previous view."}</p></section>
        <section className="doc-section" id="platform"><h2>05. {de ? "Plattform verbinden" : "Connect the platform"}</h2><ol><li>{de ? "Öffnen Sie /config → Manage Platform." : "Open /config → Manage Platform."}</li><li>{de ? "Tragen Sie die URL Ihrer Anomx-Instanz ein und melden Sie sich mit Ihrem Plattformkonto an." : "Enter your Anomx instance URL and sign in with your platform account."}</li><li>{de ? "Die Plattform stellt einen eigenen CLI-Agent-Token aus. Anfragen bleiben an Ihren Nutzer- und Organisationskontext gebunden." : "The platform issues a dedicated CLI-agent token. Requests stay linked to your user and organization context."}</li></ol><p>{de ? "Der verbundene Agent kann Plattformdaten abrufen, Jobs und Systeme untersuchen und mit den freigegebenen APIs arbeiten. Die lokale CLI und der Hintergrundagent teilen die Agentengrundlage, haben aber unterschiedliche Ausführungsrichtlinien." : "The connected agent can retrieve platform data, investigate jobs and systems, and work through permitted APIs. The local CLI and background agent share the agent foundation but have different execution policies."}</p></section>
        <section className="doc-section" id="background"><h2>06. {de ? "Hintergrundagent" : "Background agent"}</h2><p>{de ? "Richten Sie in der Plattform einen geplanten Prompt ein. Beschreiben Sie, was beobachtet werden soll, mit welchem Rhythmus und welche Ergebnisse hilfreich sind. Hintergrundläufe arbeiten ohne interaktive Fragen oder Freigabedialoge." : "Configure a scheduled prompt in the platform. Describe what to monitor, the cadence, and the results that would be useful. Background runs work without interactive questions or approval dialogs."}</p><h3>{de ? "Was der Agent tun darf" : "What the agent can do"}</h3><ul><li>{de ? "Daten lesen, den Systemzustand untersuchen und frühere Hintergrundläufe prüfen." : "Read data, investigate system state, and inspect previous background runs."}</li><li>{de ? "Standardmäßig Empfehlungen erstellen, statt eigenständig operative Änderungen durchzuführen." : "Create recommendations by default, instead of making operational changes directly."}</li><li>{de ? "Plattformobjekte nur dann direkt erstellen, ändern oder löschen, wenn das Team dies für den jeweiligen Objekttyp ausdrücklich freigegeben hat." : "Directly create, update, or delete platform objects only when the team explicitly allows that operation for the object type."}</li></ul><h3>{de ? "Rechte, Budgets und Verlauf" : "Permissions, budgets, and history"}</h3><p>{de ? "Bestehende Zugriffsrechte gelten weiter. Zugangsdaten und Automatisierungsrechte können vom Hintergrundagenten nicht geändert werden. Stündliche und tägliche Tokenlimits können Läufe pausieren; pausierte Arbeit wird später erneut geprüft. Frühere Durchläufe helfen, doppelte Findings zu vermeiden." : "Existing access permissions still apply. Background agents cannot change credentials or automation permissions. Hourly and daily token limits can pause runs; paused work is rechecked later. Earlier runs help avoid duplicate findings."}</p><p className="doc-note">{de ? "Background ist kein unbeaufsichtigter Vollzugriff auf Maschinen. Datei- und Prozessänderungen sowie beliebige Host-Kommandos gehören nicht zu diesem Modus. Für direkte Steuerung braucht es passende Integrationen und ausdrücklich freigegebene Ausführungspfade." : "Background is not unattended full control of machines. File and process changes and arbitrary host commands are outside this mode. Direct control requires appropriate integrations and explicitly authorized execution paths."}</p></section>
        <section className="doc-section" id="storage"><h2>07. {de ? "Sitzungen & Speicher" : "Sessions & storage"}</h2><p>{de ? "Der Agent speichert seinen Zustand standardmäßig unter ~/.anomx. ANOMX_HOME oder --home ändern diesen Pfad." : "The agent stores state under ~/.anomx by default. ANOMX_HOME or --home overrides this location."}</p><CopyCommand command="anomx --print-home" /><div className="doc-table-wrap"><table className="doc-table"><thead><tr><th>{de ? "Pfad" : "Path"}</th><th>{de ? "Inhalt" : "Contents"}</th></tr></thead><tbody>{(de ? [["config.toml","Einstellungen und Modellauswahl"],["auth.json","Anbieter- und Plattformauthentifizierung"],["brain/","Dauerhafte lokale Erinnerungen"],["skills/","Eigene Skills"],["sessions/","Sitzungstranskripte, nach Datum geordnet"],["session_index.jsonl","Index der gespeicherten Sitzungen"]] : [["config.toml","Settings and model selection"],["auth.json","Provider and platform authentication"],["brain/","Durable local memories"],["skills/","Custom skills"],["sessions/","Session transcripts organized by date"],["session_index.jsonl","Index of saved sessions"]]).map(([name,body]) => <tr key={name}><td><code>{name}</code></td><td>{body}</td></tr>)}</tbody></table></div><p className="doc-note">{de ? "auth.json und Sitzungstranskripte können vertrauliche Daten enthalten. Nehmen Sie sie nicht in öffentliche Repositories oder unbereinigte Support-Uploads auf." : "auth.json and session transcripts may contain sensitive data. Keep them out of public repositories and unredacted support uploads."}</p></section>
        <section className="doc-section" id="package"><h2>08. Python API</h2><p>{de ? "Das Paket stellt wiederverwendbare Komponenten für Forecasting, Reconstruction und Representation bereit. Plattform-Orchestrierung und Datenpersistenz bleiben in der Plattform." : "The package provides reusable components for forecasting, reconstruction, and representation. Platform orchestration and persistence remain in the platform."}</p><CopyCommand label={de ? "Isolierte Beobachtungen erkennen · synthetische Beispieldaten" : "Detect isolated observations · synthetic example data"} command={'import pandas as pd\nfrom anomx.components.models import IsolationForestModel\n\ntraining = pd.DataFrame({\n    "temperature": [20.0, 20.2, 19.9, 20.1, 20.3, 19.8],\n    "flow": [10.0, 10.1, 9.9, 10.2, 10.0, 9.8],\n})\nobservations = pd.DataFrame({\n    "temperature": [20.1, 35.0],\n    "flow": [10.1, 2.0],\n})\nmodel = IsolationForestModel({"random_state": 42})\nmodel.fit(training)\nresult = model.predict(observations)\nprint(result[["model_score", "model_prediction"]])'} /><p>{de ? "Größere model_score-Werte sind ungewöhnlicher. model_prediction liefert 1 für Inlier und −1 für Outlier. Dieses kleine Beispiel illustriert die API; produktive Modelle benötigen repräsentative Trainingsdaten und eine eigene Validierung." : "Higher model_score values are more unusual. model_prediction is 1 for inliers and −1 for outliers. This small example illustrates the API; production models need representative training data and their own validation."}</p><h3>{de ? "Optionale Modellbibliotheken" : "Optional model libraries"}</h3><CopyCommand command={'pip install "anomx[darts]"\npip install "anomx[torch]"'} /><p>{de ? "Darts erweitert Forecasting-Workflows; PyTorch wird für TorchAutoencoderModel benötigt. PCA-Rekonstruktion und Isolation Forest gehören zu den Standardkomponenten." : "Darts extends forecasting workflows; PyTorch is required for TorchAutoencoderModel. PCA reconstruction and Isolation Forest are standard components."}</p><p><Link href="/#science">{de ? "Die drei Ansätze interaktiv entdecken" : "Explore the three approaches interactively"} →</Link></p></section>
    </article></div></Experience>;
}
